<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheet/style.css"></link>
    <title>Pantry Index</title>
  </head>
  <body>
    <body class="index-body">
    <%- include('../partials/_navbar.ejs') %>
    <div class="index-container">
    <div class="text-container">
    <h1>Other Pantries</h1>
    <p>Let's see what other Chefs are stocking!</p>
    <ul>
    <% users.forEach(user => { %>
      <li>
        <strong><%= user.username %></strong>
        <% if (user.foods && user.foods.length) { %>
          <ul>
            <% user.foods.forEach(food => { %>
              <li><%= food.name %></li>
            <% }); %>
          </ul>
        <% } else { %>
          <span>No foods listed.</span>
        <% } %>
      </li>
    <% }); %>
  </ul>
  </body>
</html>


